{
  "name": "mars-elm",
  "version": "0.1.0",
  "description": "description for mars-elm",
  "repository": "https://github.com/user/project.git",
  "scripts": {
    "start": "elm-live --output=dist/app.js src/Main.elm --pushstate --debug",
    "build": "elm-make --output=dist/app.js src/Main.elm",
    "test": "elm-test"
  }
}
